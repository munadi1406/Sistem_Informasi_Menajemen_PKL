

export default function SideNavSkeleton() {
    return (
        <aside
            className={`bg-gray-600 lg:block hidden animate-pulse fixed inset-0 z-50 my-4 ml-4 h-[calc(100vh-32px)] w-72 rounded-xl transition-transform duration-300 xl:translate-x-0`}
        >
        </aside>
    )
}
